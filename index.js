/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {App as ComponentsApp} from './src/ComponentsApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ComponentsApp);
